Basic function to tokenise a string of words which has no delimiters.

Run the script and then pass a string to the `splitter()` function. The function will output the possible ways the string can be tokenised into English words.

The list of English words taken from http://www.curlewcommunications.co.uk/wordlist.html